name: Build and deploy docs

on:
  push:
    branches: [main]
    paths:
      - "library/**"
      - "bin/**"
      - "docs/**"
      - "zensical.toml"
      - "bin/docs.py"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Regenerate API docs
        run: python3 bin/generate-docs

      - name: Build docs site
        run: uv run bin/docs.py

      - name: Write CNAME
        run: echo "framework.offline-lab.com" > docs/public/CNAME

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/public/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
